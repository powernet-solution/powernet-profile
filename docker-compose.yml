services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: powernet-profile
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
      - NEXT_TELEMETRY_DISABLED=1
